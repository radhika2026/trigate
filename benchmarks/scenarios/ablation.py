"""Ablation study benchmark (M5 stub)."""
from __future__ import annotations


async def run() -> dict:  # type: ignore[type-arg]
    """Ablation study: measure impact of each component (M5)."""
    return {}
